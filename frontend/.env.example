# Example env for frontend (do NOT commit real secrets)
# Set the backend base url used during build
VITE_BACKEND_URL=https://passop-mongo-1-283r.onrender.com

# If you need a client-side secret for local dev (not recommended), set it here locally only
# VITE_SECRET_KEY=your_local_key_here
